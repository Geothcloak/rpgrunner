using UnityEngine;
using System.Collections;

public class MoveBlocks : MonoBehaviour {
	//check to see if there is at least 1 move
	//if not then reshuffle board
	//select a sprite to highlight it
	//select itself to deselect
	//select only one next to it to swap them
	//if swap is good then delete and respawn blocks
	//if swap is bad then flip back
	//if swap is good deal damage to enemy based on score + damage multipliers
	//enemy will attack at an interval take damage - armor
	//win condition or lose condition 	
	// Use this for initialization
	
	bool firstSelection = true;
	//array[2] firstBlock
	//array[2] secondBlock 
	void Start () { 
	}
	
	void Update () {
	
	} 
	//void ResetBoard()
		//code to just auto fill all the rows and collums
	//void BlockTouch()when you select a sprite highlight it()
		//temp array < take in data
		//if (firstSelection)
			//Code to highlight block
			//firstBlock = temp
			//giveAccess(firstBlock)
		//else if (firstSelection == false)
			//if current selection == current selection
				//turn off highlight from firstBloack
				//firstSelection = true
			//else
				// if (nextTo)
					//swap blocks(firstBlock, tempArray)
				//else
					//shake animation
					//firstSelection = true;
					//turn off highlight;
	
	//GiveAccess(firstBl, tempArray)
		//temp array left[-1][0],right[+1][0],up[0][1],down[0][-1]
		
		//firstBl - left = left
		//if left[1][] >= 1
			//Highlight(left)
		
		//firstBl - right = right
		//if right[1][] <= 8
			//Highlight(right)

		//firstBl - up = up
		//if up[][1] <= 8
			//Highlight(up)
		
		//firstBl - down = down
		//if down[][1] >= 1
		//	Highlight(down)

	//Highlight(int array where)
	//	temp float array[2] =	CoordsOfBlock(where)
	//	instantiate game object highlight (temp.x, temp.y, 0f)

	//CoordsOfBlock(where){
	//	float array temp[2]
	//	switch where[1]
	//	case 1 : (number that represents x position)
	//	case 2 : 
	//
	//	switch where[][1]
	//	case 1 : (number that represents y position)
	//
	//	return(temp[2])
		
		
				
	//SwapBlocks(firstBlock, tempArray)
	//	tempSwapArray
	//	if tempArray Highlighted
	//		tempSwapArray = tempArray
	//		tempArray = firstBlock
	//		firstBlock = tempSwapArray
	//
	//
	//
	//fillBlocks	
	//
}
